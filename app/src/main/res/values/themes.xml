<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme._12schoolAdminPanel" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:statusBarColor">@color/black</item>



    </style>
<style name="Theme.Collapsingtoolbar" parent="Base.Theme._12schoolAdminPanel">
    <item name="android:textColor">@color/textColorWhite</item>
    <item name="android:textColorPrimary">@color/textColorWhite</item>
    <item name="android:textStyle">bold</item>
</style>
    <style name="circleImageView" parent="Base.Theme._12schoolAdminPanel">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">50%</item>
    </style>
    
    <style name="Theme._12schoolAdminPanel" parent="Base.Theme._12schoolAdminPanel" />
</resources>